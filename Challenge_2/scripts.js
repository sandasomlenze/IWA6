const rent = 400;
const tax = 8
const food = 51.7501;
const salary = 800;
const transport = 10.2;
const hourOfDay = 00;
const minuteOfDay = 00;

// Only change below this line



const taxAsDecimal = 8 / 100 * 800
  const startingAfterTax = 800 * 1 - taxAsDecimal
	const balance = startingAfterTax - transport - food - rent
const R = 'R'

    if(hourOfDay == '00' && minuteOfDay == '00'){
        console.log(R,balance.toFixed(2))
    }
        else (hourOfDay && minuteOfDay !== null) && (hourOfDay == '00' && minuteOfDay == '00') 

    
